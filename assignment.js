// simple interst
let principal=Number(prompt("enter ammount"));
let rate=Number(prompt("enter rate"));
let time=Number(prompt("time period"));
alert((principal*rate*time)/100);

//VOLUME OF CUBE
let edge=Number(prompt("enter edge"));
alert((6*edge*edge));

// surface Area of Cylinder

let Radius=Number(prompt("enter Radius"));
let height=Number(prompt("enter Height"));
alert((2*Math.PI*Radius*height));

// area of circle

let radius=Number(prompt("enter radius"));
alert((Math.PI*radius*radius));